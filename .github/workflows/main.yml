name: Atualizar Radar Mineral

on:
  schedule:
    - cron: '0 9 * * *' # Roda todo dia às 06h (horário de Brasília)
  workflow_dispatch: # Permite que você rode manualmente se quiser

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Instalar dependências
        run: pip install pandas requests beautifulsoup4 google-generativeai

      - name: Rodar o Robô de Notícias
        env:
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
        run: python robo_geologico.py

      - name: Salvar mudanças
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          git add .
          git commit -m "Atualização automática: $(date)" || exit 0
          git push
